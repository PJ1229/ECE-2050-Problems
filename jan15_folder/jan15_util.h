#include <vector>
#include <algorithm> // allow you to use max(a,b), which returns the larger one among a and b

using namespace std;


int largestNum(vector<int>& v);
int largestNumRec(vector<int>& v);
